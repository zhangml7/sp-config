spring:
  application:
    name: item-service
server:
  port: 8001

eureka:
  client:
    service-url:
             #部署地点信息
      defaultZone: http://eureka1:2001/eureka,http://eureka2:2002/eureka
spring:
  cloud:
    config:
      override-none: true     